"use client";import {useTransition} from "react";import {toggleShopping} from "@/app/actions";export default function ShoppingCheck({weekKey,ingredientId,checked}:{weekKey:string;ingredientId:string;checked:boolean}){const [pending,start]=useTransition();return <input aria-label="Acheté" disabled={pending} type="checkbox" defaultChecked={checked} onChange={e=>start(()=>toggleShopping(weekKey,ingredientId,e.target.checked))}/>}
