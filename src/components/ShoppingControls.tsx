"use client";import {useState} from "react";export default function ShoppingControls({text}:{text:string}){const [hide,setHide]=useState(false);async function share(){if(navigator.share)await navigator.share({title:"Liste de courses",text});else{await navigator.clipboard.writeText(text);alert("Liste copiée dans le presse-papiers")}}return <div className="actions no-print"><button className="btn" onClick={share}>Partager la liste</button><button className="btn alt" onClick={()=>{setHide(!hide);document.body.classList.toggle("hide-checked")}}>{hide?"Afficher tous les articles":"Masquer les articles cochés"}</button><span className="pill">Impression : Ctrl/Cmd + P</span></div>}
