import {prisma} from "@/lib/prisma";import RecipeForm from "@/components/RecipeForm";export const dynamic="force-dynamic";export default async function NewRecipe(){const ingredients=await prisma.ingredient.findMany({orderBy:{name:"asc"}});return <><section className="hero"><p className="eyebrow">Création</p><h1>Nouvelle recette</h1></section><RecipeForm ingredients={ingredients}/></>}
