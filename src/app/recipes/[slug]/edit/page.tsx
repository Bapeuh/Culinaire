import {notFound} from "next/navigation";import {prisma} from "@/lib/prisma";import RecipeForm from "@/components/RecipeForm";export const dynamic="force-dynamic";export default async function Edit({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const [recipe,ingredients]=await Promise.all([prisma.recipe.findUnique({where:{slug},include:{ingredients:{orderBy:{order:"asc"}},steps:{orderBy:{order:"asc"}}}}),prisma.ingredient.findMany({orderBy:{name:"asc"}})]);if(!recipe)notFound();return <><section className="hero"><p className="eyebrow">Édition</p><h1>{recipe.name}</h1></section><RecipeForm ingredients={ingredients} initial={recipe}/></>}
