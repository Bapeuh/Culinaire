import {notFound} from "next/navigation";import Link from "next/link";import {prisma} from "@/lib/prisma";import RecipeView from "@/components/RecipeView";export const dynamic="force-dynamic";
export default async function Recipe({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const recipe=await prisma.recipe.findUnique({where:{slug},include:{ingredients:{include:{ingredient:true,component:true},orderBy:{order:"asc"}},steps:{include:{component:true},orderBy:{order:"asc"}}}});if(!recipe)notFound();return <><section className="hero"><p className="eyebrow">{recipe.category}</p><h1>{recipe.name}</h1><p className="muted">{recipe.description}</p><div className="meta"><span className="pill">Préparation {recipe.prepMinutes} min</span><span className="pill">Cuisson {recipe.cookMinutes} min</span>{recipe.restMinutes>0&&<span className="pill">Repos {recipe.restMinutes} min</span>}</div><div className="actions no-print" style={{marginTop:16}}><Link className="btn alt" href={`/recipes/${slug}/edit`}>Modifier</Link><span className="pill">Impression : Ctrl/Cmd + P</span></div></section><RecipeView recipe={recipe}/></>}
